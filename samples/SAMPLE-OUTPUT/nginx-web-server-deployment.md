# NGINX Web Server Deployment

## Overview

Deploy and configure NGINX web server with SSL/TLS support and custom site configuration

**Hosts**: `webservers`



## Parameters


| Parameter | Description |
|-----------|-------------|

| `nginx_port Port number for HTTP traffic (default` | 80) |

| `nginx_ssl_port Port number for HTTPS traffic (default` | 443) |



## Warnings


> ⚠️ **Important Notices:**
> 

> - Ensure firewall rules allow traffic on specified ports

> - SSL certificates must be provided separately

> - This will override any existing configuration for the same site

> - Only applies when enable_ssl is true



## Usage Examples



```yaml
ansible-playbook deploy-nginx.yml -e "site_name=example.com enable_ssl=true"
```





## Tasks

### Pre-Tasks

No pre-tasks defined.


### Main Tasks


- **Update apt cache** (*apt*)
  Condition: `ansible_os_family == "Debian"`
  
- **Install nginx** (*package*)
  
  
- **Create document root** (*file*)
  
  
- **Configure nginx site** (*template*)
  
  
- **Enable nginx site** (*file*)
  
  
- **Start nginx service** (*service*)
  
  
- **Allow HTTP traffic** (*ufw*)
  Condition: `ansible_os_family == "Debian"`
  
- **Allow HTTPS traffic** (*ufw*)
  Condition: `['ansible_os_family == "Debian"', 'enable_ssl | bool']`
  


### Post-Tasks

No post-tasks defined.


### Handlers


- **Reload nginx** (*service*)



## Execution Flow

```mermaid
graph TD
    Start([Start])

    subgraph MainTasks["Tasks"]
    
        T0["Update apt cache"]
    
        T1["Install nginx"]
    
        T2["Create document root"]
    
        T3["Configure nginx site"]
    
        T4["Enable nginx site"]
    
        T5["Start nginx service"]
    
        T6["Allow HTTP traffic"]
    
        T7["Allow HTTPS traffic"]
    
    end

    Start --> MainTasks



    subgraph Handlers["Handlers"]
    
        H0["Reload nginx"]
    
    end

    
    MainTasks
    --> End([Complete])
```


---

*Documentation generated by Anodyse v0.1.0*

