---
- name: Reload systemd
  systemd:
    daemon_reload: yes

- name: Restart application
  systemd:
    name: "{{ app_service_name }}"
    state: restarted

- name: Stop application
  systemd:
    name: "{{ app_service_name }}"
    state: stopped
