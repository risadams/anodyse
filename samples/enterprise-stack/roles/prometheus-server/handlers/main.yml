---
- name: Reload systemd
  systemd:
    daemon_reload: yes

- name: Restart prometheus
  systemd:
    name: prometheus
    state: restarted

- name: Reload prometheus
  systemd:
    name: prometheus
    state: reloaded
